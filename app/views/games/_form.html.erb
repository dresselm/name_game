<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :winning_contestant_id %><br>
    <%= f.number_field :winning_contestant_id %>
  </div>
  <div class="field">
    <%= f.label :losing_contestant_id %><br>
    <%= f.number_field :losing_contestant_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
