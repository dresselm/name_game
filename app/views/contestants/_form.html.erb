<%= form_for(@contestant) do |f| %>
  <% if @contestant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contestant.errors.count, "error") %> prohibited this contestant from being saved:</h2>

      <ul>
      <% @contestant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name_id %><br>
    <%= f.number_field :first_name_id %>
  </div>
  <div class="field">
    <%= f.label :middle_name_id %><br>
    <%= f.number_field :middle_name_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
